[project]
name = "ncs"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.3.3",
    "pod5>=0.3.35",
    "pywavelets>=1.9.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.13",
]
